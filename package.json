{
  "name": "cunn.ing/work",
  "version": "0.1.0",
  "description": "Cunning Work - server workers backend for VSCode",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "node src/build.js",
    "watch": "node src/build.js --watch",
    "start": "node src/build.js --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["js", "vscode", "server-workers"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "codemirror": "^6.0.1",
    "esbuild": "^0.20.0"
  }
}
